<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nbamir.sda1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProofActivity"></activity>
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".UserAccounts.SignupActivity" />
        <activity android:name=".UserAccounts.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavigationDrawer.NavigationDrawer" />
        <activity android:name=".BookmarksActivity" />
        <activity android:name=".ItemBoard.EventBoardActivity"/>
        <activity android:name=".EventMaker.AddItemActivity" />
        <activity android:name=".EventMaker.ItemPageActivity" />
    </application>

</manifest>